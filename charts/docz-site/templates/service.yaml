apiVersion: v1
kind: Service
metadata:
  name: {{ include "docz-site.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "docz-site.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docz-site.selectorLabels" . | nindent 4 }}
